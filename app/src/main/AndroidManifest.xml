<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projekt7">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Projekt7">
        <activity
            android:name=".SaveSpotActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="false" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCHs6x4ABggCDaAZHM42HMLDwmAx24aLV0" />

        <activity
            android:name=".CreateMapActivity"
            android:exported="false"
            android:label="@string/title_activity_create_map" />
        <activity
            android:name=".DisplayMapsActivity"
            android:exported="false"
            android:label="@string/title_activity_display_maps" />
        <activity
            android:name=".UploadImageActivity"
            android:exported="false" />
        <activity
            android:name=".ProfileScreenActivity"
            android:exported="false" />
        <activity
            android:name=".RUMapsActivity"
            android:exported="false"
            android:label="@string/title_activity_rumaps" />
        <activity
            android:name=".LoginScreenActivity"
            android:exported="false" />
        <activity
            android:name=".BOProfileActivity"
            android:exported="false" />
        <activity
            android:name=".CreateBOActivity"
            android:exported="false" />
        <activity
            android:name=".CreateRUActivity"
            android:exported="true" />
        <activity
            android:name=".ChooseUserActivity"
            android:exported="false" />
        <activity
            android:name=".SavePlaceActivity"
            android:exported="false" />
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>